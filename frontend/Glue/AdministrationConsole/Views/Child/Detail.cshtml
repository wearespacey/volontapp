@model Child

<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">@Model.Id</h5>
        <p class="card-text">@Model.FirstName @Model.LastName</p>
        <a href="/case/create/@Model.Id" class="btn btn-primary">Ajouter cas de dissparition</a>
    </div>
</div>